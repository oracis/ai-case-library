# 五源信息渠道改造方案

> 调研日期：2026-09-13（全部结论来自本机实测，探针原始响应留在 `_probe/src/`、`_probe/p*.py`）
> 目标：把「拆解海外」案例库的采集层，从「各源零散抓首页」升级为「一源一策、API 优先、详情页兜底、来源可追溯」

---

## 0. 一句话结论

| 源 | 等级 | 官方 API | 详情页可达 | 本方案定位 | 改造量 |
|---|---|---|---|---|---|
| TrustMRR | A | ✅ `/api/ai` 免鉴权 | ✅ HTML + `.md` | **数字地基**（已落地，补 `.md`） | 小 |
| IndieHackers | B | ❌ 无 | ✅ 服务端渲染 | **方法论来源**（新增） | 中 |
| Product Hunt | B | ⚠️ 仅 Atom feed | ❌ Cloudflare 403 | **雷达，只做发现**（降级） | 小 |
| Hacker News | B | ✅ Algolia 免 key | ✅ `items/<id>` | **创始人自曝原文**（补详情） | 小 |
| ARR Club | B- | ❌ 企业版才开放 | ✅ JSON-LD FAQPage | **ARR 时间点校对**（新增） | 中 |

**核心结论：五个源里，只有 TrustMRR 能拿到「支付网关验证过的收入」。其余四个全部是自报或二手转述，必须在数据层显式标注，不能混进同一个「收入」字段。**

---

## 1. 实测证据总表

所有以下结论均为本机 `urllib` 直连实测，非文档推断。

| 源 | 端点 | HTTP | 关键发现 |
|---|---|---|---|
| TrustMRR | `GET /api/ai` | 200 · 70,926B | 顶层 `{metricAccess, recentlyListedStartups[25], bestDeals[25]}`，**单条 40 字段** |
| TrustMRR | `GET /startup/<slug>.md` | 200 · 10,742B | 官方 AI 读本；含 **Verified / user-generated 出处分层**、refresh 时间戳、创始人 X 粉丝数 |
| TrustMRR | `GET /startup/<slug>` | 200 | JSON-LD `Organization.url` = 官网明文 |
| Hacker News | `GET hn.algolia.com/api/v1/search` | 200 | 免 key；连打 6 次无 `X-RateLimit-*`、无 429；支持 `numericFilters=points>50` |
| Hacker News | `GET hn.algolia.com/api/v1/items/<id>` | 200 | 返回**完整评论树**，`children[].text` 是 HTML 正文 |
| IndieHackers | `GET /product/<slug>` | 200 · 133KB | **服务端渲染**；`<a class="product-metrics__stat--revenue">Revenue｜$125K｜/mo</a>` 可直接正则 |
| IndieHackers | `GET /sitemap.xml` | 200 | **5 个分片**，第 3 片 50,000 条 → **11,523 个唯一产品 slug** |
| IndieHackers | `GET /products?sorting=highest-revenue` | 200 · 87KB | 有按收入排序的列表页（但**分页是前端 JS，`?page=2` 返回相同字节**） |
| IndieHackers | `GET /product/<slug>` (JSON) | 404 | **没有 JSON API**，`.json` / `/api/product/` 均 404 |
| Product Hunt | `GET /feed` | 200 | **唯一可达通道**；Atom 50 条；含 title/published/link/content/author |
| Product Hunt | `GET /feed?category=artificial-intelligence` | 200 | **分类过滤可用** |
| Product Hunt | `GET /products/<slug>`、`/products`、`/leaderboard/...`、`/rss` | **403** | 全部 Cloudflare「Just a moment...」 |
| Product Hunt | `GET api.producthunt.com/v2/api/graphql` | 404 | 需 token，无 token 不可用 |
| ARR Club | `GET /robots.txt` | 200 | `Allow: /`，仅禁 `/admin` `/api/admin` `/api/auth` `/login` `/account` |
| ARR Club | `GET /sitemap.xml` | 200 | 3,889 条 → **1,002 个真实公司页**（`www.arr.club/<slug>`） |
| ARR Club | `GET /<slug>` | 200 · 100KB | **4 个 JSON-LD 块**；`FAQPage` 内含**带年份的 ARR** |
| ARR Club | `GET /stripe` | **404** | 并非全覆盖，sitemap 是唯一可靠枚举 |
| ARR Club | `arr` RSC 字段 | 缺失 | `/notion` 上**取不到**；`arr` 不可作主通道 |

---

## 2. 每源详查

### 2.1 TrustMRR —— A 级，数字地基

**为什么是 A**：收入由支付网关（Stripe 等）侧验证，`.md` 里明确写着
`Data identified in the Verification Sources section below is verified through connected external providers and APIs.`
——这是五个源里**唯一**有这层背书的。

**三条通道，按可信度递减使用：**

1. **`GET /api/ai`（主通道，已落地）** —— 免鉴权，一次拿 50 条 × 40 字段。
   字段清单（实测）：
   ```
   slug name url website description icon category country
   revenue{mrr,last30Days,total}  customers  activeSubscriptions
   askingPrice  previousAskingPrice  multiple  profitMarginLast30Days
   growth30d  growthMRR30d  rank  listingTier  onSale  offerCount
   visitorsLast30Days  pageviewCount  revenuePerVisitor
   googleSearchImpressionsLast30Days  foundedDate  xHandle  xFounderName
   xProfilePicture  markdownUrl  stealthMode  isMobileApp  mobileApp
   targetAudience  paymentProvider  firstListedForSaleAt
   brandingPrimaryColor  brandingSecondaryColor
   listingTierBgColor  listingTierBgColorDark
   ```
2. **`GET /startup/<slug>.md`（P1 待补）** —— 10.7KB 纯文本，比 JSON 多出：
   - **出处分层**（哪些字段 verified、哪些 user-generated）
   - `refresh 时间戳`（如 `next refresh expected around 2026-09-17T15:17:48.580Z`）→ 可判断数据新鲜度
   - 收入全时段表 / 日活 30 天 / 月度时序
   - `X followers`（创始人影响力，JSON 里没有）
   - 可解析的推荐 JSON
3. **`GET /startup/<slug>`（兜底）** —— JSON-LD `Organization.url` 拿官网明文。

**过滤规则（已落地）**：`stealthMode` 跳过；`max(mrr, last30Days) < $100` 跳过。

**改造结论**：主通道已完成，P1 只需把 `.md` 接上，用于**给精写案例补「出处分层」和「数据新鲜度」两个字段**。

---

### 2.2 IndieHackers —— B 级，唯一的方法论来源

**为什么是 B**：用户原话「唯一能拿到『他们怎么做到的』的地方，但全是自报数字」。实测证实：
- 收入是**自报**，但**服务端渲染**，能直接抓；
- 里程碑时间线里是**创始人亲笔长文**（如 `We've passed $20K MRR!`），这是别的源都没有的「过程」素材。

**通道（无 API，只能 HTML）：**

1. **枚举**：`GET /sitemap.xml` → 5 个分片（Google Storage 托管）
   - 第 3 片：50,000 条 → **11,523 个唯一产品 slug**
   - URL 有两种形态，必须区分：
     - `/product/<slug>` —— **干净产品页，133KB，有完整数据** ← 要这个
     - `/product/<slug>/<milestoneId>` —— 单条里程碑，42KB ← 忽略
2. **详情页解析**：`GET /product/<slug>`
   - 收入：`<a class="product-metrics__stat product-metrics__stat--revenue" href="/product/<slug>/revenue">` 内为 `Revenue｜$125K｜/mo`
   - 官网 / Twitter / Facebook：同级的 `product-metrics__stat` 链接
   - 帖子数：`Posts｜30+`
   - 「怎么做到的」：正文时间线长文
3. **补充页**：`GET /product/<slug>/revenue`（30KB）

**⚠️ 重要限制**：`/products?page=2` 与 `/products?sorting=highest-revenue` 返回**相同字节**（86,564 vs 86,570 vs 86,917）——分页与排序都是**前端 JS** 实现的，`?page=` 参数无效。**枚举只能靠 sitemap，不能靠列表页翻页。**

**改造结论**：新增 `harvest_indiehackers()`。
- 用 sitemap 第 3 片做枚举（**只取干净 slug，丢掉里程碑 URL**）
- 用 `product-metrics__stat--revenue` 正则取收入
- **强制打标 `self_reported`**，并写入 `metrics.self_reported = true`
- 「怎么做到的」长文抽出来存 `metrics.story_excerpt`，供精写阶段用
- 限流：实测无速率限制，但**建议 1.5s 间隔 + 单轮上限 40 条**（礼貌抓取）

---

### 2.3 Product Hunt —— B 级，只能当雷达

**这是本次调研最硬的负面结论，必须写清楚：**

- `GET /products/<slug>` → **403 Cloudflare**
- `GET /products` → **403**
- `GET /leaderboard/daily/2026/9/12` → **403**
- `GET /rss` → **403**
- `GET /topics/*` → **403**
- `GET /frontend/graphql` → **403**
- `GET api.producthunt.com/v2/api/graphql` → **404（需 token）**

**唯一可达：`GET /feed`（Atom，200，43KB，50 条/页）**

用户要求「如果有详情页面，必须也抓到详情页面的信息」——**对 Product Hunt 这条在技术上做不到**。
详情页全部被 Cloudflare 拦下，服务端拿不到。**必须如实说明，不假装能抓。**

`/feed` 单条实际内容（原始 XML）：
```xml
<id>tag:www.producthunt.com,2005:Post/1251186</id>
<published>2026-09-15T02:56:57-07:00</published>
<link rel="alternate" href="https://www.producthunt.com/products/appwrite"/>
<title>Appwrite 2.0</title>
<content type="html">
  The open-source cloud for agents and developers
  <a href=".../products/appwrite?...">Discussion</a> | <a href=".../r/p/1251186?app_id=339">Link</a>
</content>
<author><name>Eldad Fux</name></author>
```

**能拿到**：产品名、简介、发布日、详情页 URL、作者名、分类（用 `?category=` 过滤）
**拿不到**：**upvote 数**（榜单热度的唯一指标，feed 里没有）

**改造结论**：PH **降级为纯发现通道**。
- 保留 `/feed` + `?category=artificial-intelligence` / `?category=developer-tools` 做新项目雷达
- 入库只给 `one_liner` + `source_url`，**不写任何收入/热度字段**
- 如果将来要 upvote，**唯一正路是申请 PH 官方 API token**（届时 `harvest_producthunt(limit, token)` 已有签名，直接填 token 即可）
- **明确写进 note：「榜单热度 ≠ 有人在付钱；本条目无收入数据」**

---

### 2.4 Hacker News —— B 级，创始人自曝原文

**为什么是 B（用户原话「技术圈最诚实的自曝渠道」）**：因为评论区才是真话所在地。

**通道（API 免 key，最省事）：**

1. **发现**：`GET hn.algolia.com/api/v1/search?query=<kw>&tags=story`
   - 字段：`title / url / points / num_comments / objectID / created_at / author`
   - **支持质量过滤**：`numericFilters=points>50`（实测有效，`nbHits=57`）
   - **实测无速率限制**：连打 6 次全部 200，无 `X-RateLimit-*`、无 429，响应 1.0–2.9s
2. **详情（P1 待补，这是关键）**：`GET hn.algolia.com/api/v1/items/<objectID>`
   - 返回**完整评论树**：`children[].text`（HTML）、`author`、`created_at`
   - 顶层字段：`author children created_at created_at_i id options parent_id points story_id text title type url`
   - 实测 `items/1` 正常返回，`children[0]` 的 `text` 有 76 字符正文

**改造结论**：`harvest_hn()` 现有实现只用 search，**补一个 `fetch_hn_comments(objectID, top_n)`**。
- 对已入选的 HN 条目，抓 `items/<id>`，取**前 N 条高赞评论的 `text`**（去 HTML 标签）
- 存入 `metrics.hn_discussion`，喂给精写阶段当「同行/用户怎么评价」
- 限流：无硬限制，但**建议 0.5s 间隔**，且只对**已入选**条目抓评论（不是全量）

---

### 2.5 ARR Club —— B- 级，时间点校对

**为什么是 B-（用户原话「查某个时间点的 ARR 方便，但大量二手转述」）—— 实测完全印证：**

- 数据来源是**转述 + 自贴「Certificate」徽章**，FAQ 里反复自引
  `according to ARR Club's verified data`（**自己说自己是 verified，这就是二手转述的特征**）
- ✅ `robots.txt` **`Allow: /`** → 可以抓
- ✅ sitemap 有 **1,002 个公司页**
- ❌ `arr` 这个 RSC 字段**不可靠**：`/notion` 上**取不到**；`/linear` 能取到
- ❌ `GET /stripe` → **404**（**并非全覆盖**）

**唯一可靠的取数通道是 `FAQPage` JSON-LD**，里面是**带年份**的结构化数据：

`GET https://arr.club/notion` → 4 个 JSON-LD 块：

| # | @type | 有用字段 |
|---|---|---|
| 0 | （`@graph` 容器） | — |
| 1 | `Organization` | `name` `description` `url`=**官网** `foundingDate` `industry` `logo` `sameAs` `knowsAbout` |
| 2 | **`FAQPage`** | **ARR 数字 + 年份 + 增速（核心）** |
| 3 | `BreadcrumbList` | 面包屑 |

`FAQPage` 实测内容（Notion）：
```
Q: What is Notion's ARR in 2025?   A: ...reached $600M ... in 2025, according to ARR Club's verified data.
Q: What is Notion's revenue in 2025? A: ...was $600M in 2025. The company has been growing at +83% YoY.
Q: What is Notion's ARR in 2024?   A: ...reached $300M ... in 2024...
Q: What is Notion's current ARR?   A: ...current ARR is $600M.
Q: What is Notion's revenue growth rate? A: ...+83% year-over-year.
```
正文另有：`Current ARR Certificate $600M`、`Milestone Timeline`、`Valuation & Funding History`、`Series D (Feb 24, 2022)`

**注意**：`<title>` 写的是 `ARR 2026: $600M`，而 FAQ 里 2025 是 $600M —— **年份口径有歧义，取数时必须连年份一起存，不能只存数字。**

**改造结论**：新增 `harvest_arr_club()`，**只做校对，不做主数据**。
- 枚举：sitemap → 1,002 个公司页（排除 `database`/`signal`/`investors`/`weekly`/`pricing`/`submit`/`rankings` 等工具页）
- 取数：只解 `FAQPage` JSON-LD 的 `mainEntity[]`，正则提 `ARR` + 年份 + 增速
- 官网：`Organization.url`
- **强制打标**：`source_kind = "secondary"`，note 写明「ARR Club 为二手转述渠道，数字需与一手来源交叉核对」
- 用途：给已有案例做 **ARR @ 时间点** 的横向校对，**不新增候选条目**（避免二手数据污染候选池）
- 限流：实测无速率限制，响应 2.6–3.9s（较慢），**建议 2s 间隔 + 单轮上限 30 条**

---

## 3. 字段映射（对齐现有 schema）

现有候选条目 schema（实测 `data/candidates.json`）：
```json
{
  "id": "eloquent", "name": "Eloquent AI", "name_en": "Eloquent AI",
  "origin": "美国", "one_liner": "...", "category": "企业服务",
  "verification": "unverified", "metrics": {"headline": "..."},
  "models": [], "note": "...", "blocking": "...", "added_at": "2026-09-11",
  "source_url": "...", "website": "...", "harvest_source": "trustmrr",
  "trustmrr_slug": "...", "founded_at": "...", "founder_x": "..."
}
```

**新增字段（统一命名，五个源共用）：**

| 字段 | 含义 | 取值 | 适用源 |
|---|---|---|---|
| `source_url` | 详情页地址 | 各源详情 URL | 全部 |
| `website` | **官网明文** | JSON-LD `url` / 页面 Website 链接 | 全部 |
| `harvest_source` | 采集通道 | `trustmrr`/`indiehackers`/`producthunt`/`hn`/`arrclub` | 全部 |
| **`source_kind`** | **来源性质** | `verified` / `self_reported` / `secondary` / `discovery` | 全部（**新增，核心**） |
| `metrics.mrr` | 月度经常性收入 | 数字 | TrustMRR |
| `metrics.all_time` | 累计收入 | 数字 | TrustMRR / IH |
| `metrics.arr_by_year` | **ARR + 年份** | `{"2025": 600000000, "2024": 300000000}` | ARR Club |
| `metrics.growth_yoy` | 同比增速 | `"+83%"` | ARR Club |
| `metrics.story_excerpt` | 「怎么做到的」摘录 | 文本 | IndieHackers |
| `metrics.hn_discussion` | 评论区要点 | 文本 | HN |
| `metrics.self_reported` | 是否自报 | `true` | IndieHackers / HN |
| `data_freshness` | 数据新鲜度 | `.md` 里的 refresh 时间戳 | TrustMRR |

**`source_kind` 是本方案最重要的新增字段**，直接对应用户给的 A/B/B- 分级：

| 值 | 含义 | 对应源 | 前端展示 |
|---|---|---|---|
| `verified` | 支付网关验证 | TrustMRR | 🟢 已验证收入 |
| `self_reported` | 创始人自报 | IndieHackers / HN | 🟡 自报数字 |
| `secondary` | 二手转述 | ARR Club | 🟠 转述数据 |
| `discovery` | 仅发现，无数字 | Product Hunt | ⚪ 仅线索 |

这样前端一眼能看出「这个数字能不能信」，而不是把所有源的数字混在一列里。

---

## 4. 改造清单（按优先级）

### P0 · 必做
1. **harvest.py 加 `SOURCE_KIND` 常量表**，五个源逐一映射到四个性质
2. **所有采集结果强制写 `source_kind`**（含存量回填脚本）
3. **Product Hunt 降级**：`harvest_producthunt()` 去掉一切收入/热度字段，只留 `one_liner` + `source_url` + `source_kind="discovery"`
4. **前端卡片按 `source_kind` 显示徽章**（`static/app.js` + `admin/app.js` 的 `cardLinks` 旁边加一个 `sourceBadge(c)`）

### P1 · 应做
5. **`harvest_indiehackers()`**：sitemap 枚举 + `product-metrics__stat--revenue` 解析 + `self_reported` 打标
6. **`harvest_arr_club()`**：sitemap 枚举 + `FAQPage` JSON-LD 解析 + `secondary` 打标，**只做校对不新增**
7. **TrustMRR `.md` 通道**：补 `data_freshness` 与出处分层
8. **HN `fetch_hn_comments()`**：抓评论区正文进 `metrics.hn_discussion`
9. **`ai_verify` 来源白名单**：剔除 `music.163.com` / `y.qq.com` 这类明显无关来源（此前污染过一次）

### P2 · 可做
10. 申请 Product Hunt 官方 API token，恢复 upvote 能力
11. ARR Club 与 TrustMRR 数据做**同公司交叉核对**（同一家两家都有 → 可信度提升）

---

## 5. 礼貌抓取与限流

| 源 | 实测限流 | 建议策略 |
|---|---|---|
| TrustMRR | 无 | `/api/ai` 单次调用；`.md` 按需，0.25s 间隔（已有） |
| Hacker News | **无**（6 连击全 200） | 0.5s 间隔；评论只对已入选条目抓 |
| IndieHackers | 无 | **1.5s 间隔 + 单轮上限 40**（sitemap 有 1.1 万条，务必设上限） |
| Product Hunt | 无（但 403 拦截） | **0.5s 间隔；遇 403 立刻停止**，不要重试轰炸 |
| ARR Club | 无 | **2s 间隔 + 单轮上限 30**（响应本身 2.6–3.9s） |

**统一 UA**（现状 `CaseLibraryHarvester/1.0` 偏简陋）：
建议改为可识别的、带联系方式的 UA，例如
`Mozilla/5.0 (compatible; CaseLibraryHarvester/1.1; +<项目主页>)`
——多个源（尤其 ARR Club / IH）对匿名爬虫的容忍度取决于是否可追溯。

**全局硬规则**：
- 单轮总计**不超过 150 个详情页请求**
- 任何源连续 3 次非 200 → **本轮跳过该源**，写进报告，不重试
- 所有详情页请求结果写 `_cache/<source>/<slug>.html`，**同一天不重复请求**

---

## 6. 测试补充

现有基线（全绿）：selftest 119 · uitest 95 · uitest --static 96 · uitest-admin 112 · test_ai_verify 32 · test_verify_rules 42 · test_build_static 38 · test_prerender 54 · test_daily_harvest 25

**新增断言：**

| 测试 | 断言 |
|---|---|
| `test_daily_harvest` | 每个源的产出**必带 `source_kind`**，且值在四枚举内 |
| `test_daily_harvest` | Product Hunt 条目**不含** `metrics.mrr` / `metrics.arr_by_year` |
| `test_daily_harvest` | IndieHackers 条目 `metrics.self_reported === true` |
| `test_daily_harvest` | ARR Club 条目 `source_kind === "secondary"` |
| `test_daily_harvest` | ARR Club 的 `arr_by_year` 必须**带年份键**（防年份丢失） |
| `uitest.js` / `uitest-admin.js` | 卡片渲染出 `source_badge`，且四种性质各自的文案正确 |
| `selftest.py` | `SOURCE_KIND` 表覆盖全部 `harvest_source` 取值（防新增源漏配） |

**测试硬约束（沿用）**：`uitest.js` 的 `countCards` 是
`(html.match(/class="card(?:\s+read)?"/g) || []).length`——改 `cardHTML` 时**不能破坏 `class="card"` 精确模式**。

---

## 7. 风险与未解

| 风险 | 影响 | 处理 |
|---|---|---|
| **Product Hunt 详情页永久 403** | 「抓详情页」要求对 PH 无法满足 | 如实降级为发现通道；要 upvote 只能申请 token |
| **IndieHackers 分页是前端 JS** | 无法靠 `?page=` 翻页 | 只走 sitemap 枚举；注意从里程碑 URL 里提干净 slug |
| **ARR Club `arr` 字段不可靠** | 直接读 RSC 会漏数（`/notion` 就是空） | **只解 `FAQPage` JSON-LD** |
| **ARR Club 年份口径歧义** | `<title>` 说 2026、FAQ 说 2025 | 数字与年份**必须成对存储** |
| **ARR Club 覆盖不全** | `/stripe` 404 | sitemap 是唯一枚举；找不到就跳过，不猜 |
| **sitemap 体量巨大** | IH 1.1 万条、ARR Club 1000 条 | 必须设单轮上限，否则一轮跑几小时 |
| **二手数据污染候选池** | ARR Club / IH 的自报数字混进「已验证」 | `source_kind` + 前端徽章双重隔离 |
| **PH 无 upvote** | 「榜单热度」指标缺失 | 已在方案中明确标注为已知缺口 |

---

## 8. 一句话给用户

**五个源里只有 TrustMRR 配叫「A」——它的收入是支付网关验过的。另外四个全是自报或转述，本方案的做法是：让它们继续提供线索和方法论，但用 `source_kind` 字段在数据层把「可信度」钉死，前端用徽章把这件事摆到用户眼前，而不是把五种来源的数字混成一锅粥。**

其中 **Product Hunt 的详情页抓取在技术上做不到**（Cloudflare 全站 403，含 GraphQL），这是本方案唯一无法满足「必须抓到详情页」要求的地方，需要你知情。

---

# 执行记录（2026-09-17 更新）

## ✅ P0 已落地

| # | 项 | 落地情况 |
|---|---|---|
| 1 | `SOURCE_KIND` 常量表 | `scripts/harvest.py`：四值 + `KIND_LABEL` + `KIND_RANK` + `source_kind_of()` |
| 2 | 所有采集结果写 `source_kind` | `main()` 里**单一权威赋值点**；另加 `infer_source()` 从 `source_url` 反推，存量用 `--fix-source-kind` 回填 |
| 3 | Product Hunt 降级 | 不写任何收入字段（有断言守着） |
| 4 | 前端徽章 | **未做**（前端展示留到下一轮；数据层已就绪） |

存量回填结果：inbox 347 条、candidates 33 条，**零缺失**。候选池里 17 条带
TrustMRR `source_url` 的修正为 `verified`（此前被误判成 `discovery`）。

## ✅ P1 已落地

| # | 项 | 落地情况 |
|---|---|---|
| 5 | `harvest_indiehackers()` | ✅ 但**改了两处设计**（见下） |
| 6 | `harvest_arr_club()` | ✅ 只解 FAQPage JSON-LD，年份与数字成对存，只写 `data/arrclub.json` |
| 7 | TrustMRR `.md` 通道 | ✅ `fetch_trustmrr_md()` → `data_freshness` + 出处分层 + X 粉丝数 |
| 8 | HN 评论正文 | ✅ `fetch_hn_comments()` → `metrics.hn_discussion`，只对新增条目抓 |
| 9 | `ai_verify` 来源黑名单 | ✅ 词典/音乐/电商域名，先过滤再取前 6 条 |

## ⚠️ 与原方案的三处偏离（都是实测逼出来的）

### 1. IndieHackers 改成「列表页优先」，sitemap 降为广度模式

原方案写的是「sitemap 枚举」。实跑发现分片**每片约 10.6MB、网络差时超过 4 分钟**，
而且干净产品 slug 均匀散在分片里（前 2MB 一个都没有），**第 1 片实测 0 个产品** ——
等于花 150 秒什么也没拿到。

改用两个列表页：

```
/products?sorting=highest-revenue   21 个产品，~88KB，1.2–3.6s
/products                           21 个产品，~88KB，1.2–3.6s
```

实测只有 `highest-revenue` 与默认（最新）两批结果不同，`newest`/`most-posts`/
`trending`/无参数四者返回同一批。**42 个产品、几秒钟**，而且高收入榜本身就是最该看的。
sitemap 保留为 `--ih-breadth`，并记住哪些分片是空的（`barren`）不再重复下载。

顺带发现一个通用坑：**`urlopen(timeout=N)` 只管 socket 空闲，不管总耗时** ——
对方慢慢滴数据时永远不超时。所以加了 `_fetch_html_bounded()` 按总时长硬截断。

### 2. Product Hunt 无法抓详情页（原方案已预警，实测确认）

`/products/<slug>`、`/products`、`/leaderboard/*`、`/rss`、`/topics/*`、
`/frontend/graphql` **全部 403 Cloudflare**，GraphQL 无 token 返回 404。
唯一可达是 `/feed`，且 **feed 里没有 upvote 数**。
「必须抓到详情页」这条对 PH 做不到，只能当发现雷达。

### 3. ARR Club 的 `arr` 字段不可靠 → 只解 FAQPage

实测 `/notion` 上 RSC 里的 `arr` 取不到（`/linear` 才有）。只有 `FAQPage` JSON-LD
是带年份的权威值。而且 `<title>` 写 2026、FAQ 写 2025 —— **数字必须与年份成对存储**，
否则会串年份。覆盖也不全（`/stripe` 404）。它自贴「Certificate」徽章、FAQ 里
自引「according to ARR Club's verified data」——自己说自己是 verified，
这正是用户判它 B- 的理由。

## 测试

新增 `scripts/test_harvest_sources.py`，**188 项，全离线**，用 `scripts/fixtures/` 里
真抓下来的响应（IH 产品页、ARR Club 公司页与 sitemap、HN 搜索、PH feed、TrustMRR `.md`）。
已接入 CI。

守的线：每条必带 `source_kind`；IH 自报必须标 `self_reported`；PH 不许带收入字段；
ARR Club 的 ARR 必须与年份成对；sitemap 里程碑 URL 要丢掉；
`sources.json` 与 `harvest.py` 的 `source_kind` **不许漂移**；抓取要有总耗时上限。

## 仍待做

- **前端徽章**（P0 第 4 项）：数据层已就绪，`static/app.js` 与 `admin/app.js`
  的卡片还没画 `source_kind` 徽章。
- Product Hunt 官方 API token（拿到才有 upvote 数）。
- ARR Club 与 TrustMRR 同公司交叉校对。

